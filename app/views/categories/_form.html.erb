<% semantic_form_for [@shop, @category] do |f| %>
  <% f.inputs do %>
    <%= f.input :parent, :as => :select, :collection => categories_collection %>
    <%= f.input :name %>
  <% end %>
  <%= f.buttons %>
<% end %>
