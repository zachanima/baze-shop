<% semantic_form_for([@shop, @email_address]) do |f| %>
  <% f.inputs do %>
    <%= f.input :name %>
    <%= f.input :text, :label => 'Email address' %>
    <%= f.input :net, :label => 'Receive net prices' %>
  <% end %>
  <%= f.buttons %>
<% end %>
