<% semantic_form_for(@product, :html => { :multipart => true }) do |f| %>
  <% f.inputs do %>
    <%= f.input :name %>
    <%= f.input :number %>
    <%= f.input :brand %>
    <%= f.input :text %>
    <%= f.input :images, :as => :check_boxes, :collection => @product.images.collect { |i| [i.name, i.id] } %>
    <%= link_to_new('image', new_product_image_path(@product)) %>
  <% end %>
  <%= f.buttons %>
<% end %>
